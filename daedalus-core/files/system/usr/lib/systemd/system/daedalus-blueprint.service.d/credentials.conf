[Service]
# daedalus-blueprint 的凭证注入 drop-in(与其余能力服务器同款):
# secret:// 引用解析(v1 恒返 ErrSecretSourceUnavailable,预留 KWallet/credstore
# 接入位)与 LoadCredential 一致——本服务暂不消费 daedalus_token,占位保持一致。
SetCredential=daedalus_token_default:placeholder
LoadCredential=daedalus_token:/etc/credstore/daedalus_token
